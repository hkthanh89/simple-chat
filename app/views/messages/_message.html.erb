<p id="<%= dom_id message %>">
  <%= message.created_at.to_fs(:short) %>: <%= message.content %>
  <%= link_to "Delete", room_message_path(message.room, message), data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' } %>
</p>